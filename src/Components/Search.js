import React from 'react'

const Search = () => {
    return (
        <select name="source" className="form__input" style={{ marginBottom: "2rem" }}>
            <option value="" className="form__input">j</option>
        </select>
    )
}

export default Search;